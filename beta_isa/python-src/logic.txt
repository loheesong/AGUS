while game_loop:
    fn_is_win

    if is_win:
        check who win: p1 lives == 0 or p2 lives == 0 
        handle winner 
        exit game loop 

    if timer ends: p1 lives = ? != 0 , p2 lives = ? != 0 
        check who win: p1 lives >= p2 lives? p1 wins : p2 wins 
        handle winner 
        exit game loop 
    
    update values 
    wait for 0
    wait for 1 

    